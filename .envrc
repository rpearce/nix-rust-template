# Enter the flake's dev shell automatically with direnv (https://direnv.net).
# nix-direnv (https://github.com/nix-community/nix-direnv) makes this fast by
# caching the shell; plain direnv works too, just slower.
use flake
watch_file rust-toolchain.toml
